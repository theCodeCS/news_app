import {makeStyles} from '@material-ui/core/styles'

export default makeStyles({
    mainContainer: {
        marginTop: '16px',
        textAlign: 'center',
    },
    headContainer: {
        textAlign: 'center',
    }
})